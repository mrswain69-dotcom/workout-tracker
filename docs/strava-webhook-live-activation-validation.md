Production validation: manual Strava import and verification reconciliation were working, while automatic webhook provisioning returned `unavailable / missing_configuration`. This hotfix removes the incorrect dependency on optional webhook secrets while preserving subscription authentication before provider data changes. The release is green only after PR/main CI passes and a production manual sync returns `autoSync.state = active`.